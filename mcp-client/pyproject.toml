[project]
name = "mcp-client"
version = "0.1.0"
description = "A styled terminal client for connecting to MCP servers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.112.0",
    "mcp>=1.28.1",
    "python-dotenv>=1.2.2",
]
